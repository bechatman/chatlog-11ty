---
layout: base
---
{{ content | safe }}
{% for component in components %}
  {% assign url = '../_components' + component.type + component.type + '.liquid' %}
  {{ url }}
   include component.type 
{% endfor %}